# Skills layout (sketch — TODO, subject to change)

Anticipated skills, one directory each, once the protocol design settles:

- `spec/` — CEO↔CTO spec authoring and iteration; boundary design
- `spinup/` — sizing the org from the spec (hats, not headcount); launching leads
- `sprint/` — lead-side decomposition: walking skeleton, acceptance criteria, task fan-out
- `standup/` — fork-huddle standups; status files; decision log
- `escalate/` — spec-gap interpretations, amendments, instruction-vs-intent huddles
- `crystal/` — speculative-merge hook/daemon; early conflict reporting
- `retro/` — lessons-learned distillation into LESSONS.md

No skill content exists yet; the protocol documents come first.
